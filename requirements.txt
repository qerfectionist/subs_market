# --- Core ---
aiogram>=3.10.0          # The Framework. Strict 3.x
pydantic>=2.0.0
pydantic-settings>=2.2.0 # Configuration management
python-dotenv>=1.0.0

# --- Database (Async Only) ---
sqlalchemy>=2.0.28      # ORM 2.0 style
asyncpg>=0.29.0         # High-performance async driver for Postgres
alembic>=1.13.1         # Migrations
greenlet>=3.0.0         # Asyncio compatibility for SQLAlchemy

# --- Infrastructure ---
redis>=5.0.3            # FSM Storage & Caching
structlog>=24.1.0       # JSON Logging (Production Standard)
uvloop>=0.19.0; sys_platform != 'win32' # Event Loop booster (Optional but recommended for Linux)
